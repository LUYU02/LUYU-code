import React from "react"
// js 的页面是基于react的,编写一个react组件即可成为页面

export default () => {
  return (
    <div>
      <h1
        style={{ backgroundColor: "#ccc", color: "#bfa", textAlign: "center" }}>
        这是我自己写的
      </h1>
    </div>
  )
}
